<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.koitt.hona.model.product">
  	<insert id="insert-product" parameterType="Product">
  		INSERT INTO product (product_no, product_type, product_name, EA, price, inventory, explaination, attachment)
  		VALUES (#{productNo}, #{productType}, #{productName}, #{EA}, #{price}, #{inventory}, #{explaination}, #{attachment}) 
  	</insert>
  	
  	<resultMap type="Product" id="productResultMap">
  		<id property="productNo" column="product_no"/>
  		<result property="productType" column="product_type"/>
  		<result property="productName" column="product_name"/>
  		<result property="EA" column="EA"/>
  		<result property="price" column="price"/>
  		<result property="inventory" column="inventory"/>
  		<result property="explaination" column="explaination"/>
  		<result property="attachment" column="attachment"/>
  	</resultMap>
  	
  	<select id="select-product" parameterType="String" resultMap="productResultMap">
  		SELECT FROM product WHERE product_no = #{productNo}
   	</select>
  	
  	<select id="selectAll-book" resultMap="bookResultMap">
  		SELECT FROM product
  	</select>
  </mapper>